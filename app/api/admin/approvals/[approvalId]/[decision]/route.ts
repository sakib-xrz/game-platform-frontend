import { forwardMutationHeaders, proxyAdminRequest, readJson } from "@/lib/admin-bff"; import { requireAdminMutation } from "@/lib/admin-session";
export async function POST(request: Request, { params }: { params: Promise<{ approvalId: string; decision: string }> }) { const session = await requireAdminMutation(request); if (session instanceof Response) return session; const { approvalId, decision } = await params; if (!/^(approve|reject)$/.test(decision)) return Response.json({ message: "Invalid decision" }, { status: 400 }); const body = await readJson(request); return proxyAdminRequest(`/admin/approvals/${encodeURIComponent(approvalId)}/${decision}`, session, { method: "POST", headers: forwardMutationHeaders(request), body: JSON.stringify(body || {}) }); }
